# Optional WhatsApp order link (overrides built-in default when set)
# Format: https://wa.me/<country code + number, no + or spaces>
NEXT_PUBLIC_WHATSAPP_URL=https://wa.me/96551282020

# Supabase (control panel cloud storage + login)
# Create a project at https://supabase.com → Project Settings → API
# Use the Project URL and the publishable (or legacy anon) key.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Optional alias if your dashboard still labels it "anon" / "public":
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
